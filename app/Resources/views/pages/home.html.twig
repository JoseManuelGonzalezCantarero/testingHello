{% extends 'base.html.twig' %}
{% block body %}
    <div class="content">
        <h1>Bienvenido</h1>
        <p>
            Éstas son las páginas disponibles. Si no puedes visualizarlas, es porque no tienes los permisos necesarios:
        </p>
        <ol>
            <li>
                <a href="{{ path('page_route', {page: 1}) }}">Página 1</a>
            </li>
            <li>
                <a href="{{ path('page_route', {page: 2}) }}">Página 2</a>
            </li>
            <li>
                <a href="{{ path('page_route', {page: 3}) }}">Página 3</a>
            </li>
        </ol>
        <br>
        <a href="{{ path('security_logout') }}">Logout</a>
    </div>
{% endblock %}